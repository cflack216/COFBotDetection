<Window x:Class="COFBotDetection.LoginPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="600" Width="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Username:" Grid.Row="0" Margin="5"/>
        <TextBox Name="UsernameTextBox" Grid.Row="0" Margin="5" Width="200"/>
        <TextBlock Text="Password:" Grid.Row="1" Margin="5"/>
        <PasswordBox Name="PasswordBox" Grid.Row="1" Margin="5" Width="200"/>
        <Button Content="Login" Grid.Row="2" Margin="5" Width="75" Click="Login_Click"/>
        <TextBlock Name="ErrorTextBlock" Grid.Row="3" Margin="5" Foreground="Red"/>
    </Grid>
</Window>